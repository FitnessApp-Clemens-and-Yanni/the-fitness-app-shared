{
  "name": "shared",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b && git add -A && git commit -am 'update shared code' && git push"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "exports": {
    "./*": "./*"
  }
}
